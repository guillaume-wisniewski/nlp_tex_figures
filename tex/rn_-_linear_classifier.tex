\documentclass[crop,tikz]{standalone}

\input{tex/common}

% Fr labels
%\newcommand{\score}{vecteur de scores}
%\newcommand{\param}{matrice de paramètres}
%
% En labels
\newcommand{\score}{scores vector}
\newcommand{\param}{parameters matrix}


\begin{document}

    \begin{tikzpicture}

        \node[draw=airforceblue, fill=airforceblue!50, minimum width=.5cm, minimum height=6cm] (input) at (0cm,0cm) {};

        \node[draw=asparagus, fill=asparagus!50, minimum width=.5cm, minimum height=3cm] (scores) at (8cm,0cm) {};

        \draw[airforceblue] (input.north east) -- (scores.north west);
        \draw[airforceblue] (input.south east) -- (scores.south west);

        \node at (4cm, 0cm) {$\mathbf{M} \cdot \mathbf{x}$};

        \node at (8cm, -4cm) {\makecell{\score \\ (dimension: $k \times 1$)}};
        \node at (4cm, -4cm) {\makecell{\param \\ (dimension: $k \times d$)}};
        \node at (0cm, -4cm) {\makecell{observation $\mathbf{x}$ \\ (dimension: $d \times 1$)}};

    \end{tikzpicture}

\end{document}